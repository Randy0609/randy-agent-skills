# 给你的 Codex 装上三个思考 Skill

Randy 分享给伙伴与客户的一组中文工作方法：核查事实、拆解假设、审查方案。

**复制下面的安装话术，粘贴到你自己的 Codex 本地任务中。** Codex 会读取说明，下载这三个 Skill，并检查是否安装完整。

## 第一步：复制给 Codex

```text
请帮我安装 Randy 分享的三个 Skill：事实洁癖、第一性原理、对抗式审查。

请先读取这份安装指南，并按其中“给 Codex 的安装说明”执行：
https://github.com/Randy0609/randy-agent-skills/blob/main/START-HERE.md

安装到我自己的用户级 Skills 目录，供之后的任务使用。
若发现同名 Skill，先检查来源和内容；相同则保留，不同则说明差异并等我决定。
完成后列出每个 Skill 的名称、实际安装路径、文件校验结果，并告诉我怎么调用。
```

需要你的 Codex 能访问 GitHub，并能写入本机的 Skills 目录。界面如要求安装授权，按提示处理。仅把链接发到不能操作本机文件的普通网页聊天中，不会完成本机安装。

如果 Codex 无法读取 GitHub 展示页，可以让它改读同一文件的[原始文本](https://raw.githubusercontent.com/Randy0609/randy-agent-skills/main/START-HERE.md)。

## 第二步：确认安装结果

Codex 应列出下面三个名称，并确认各自目录内的 `SKILL.md` 可读取：

| Skill | 安装名称 | 主要用途 |
|---|---|---|
| 事实洁癖 | `checking-facts-ecommerce` | 交付前核查数字、证言、资质、承诺和因果说法；输出审计报告和清理版 |
| 第一性原理／第一性原则 | `first-principles` | 区分事实和惯例，拆解假设，重新推导方案和最小验证方法 |
| 对抗式审查 | `adversarial-audit` | 针对已有方案寻找具体失败路径，反证过滤后保留有证据的问题 |

**事实洁癖使用的是已公开的“电商创业者版”**，示例偏销售页、推广文案和合作材料。

官方文档说明 Codex 会自动发现新安装的 Skill。安装后先在下一条消息中尝试调用；未出现时重启 Codex。CLI / IDE 可通过 `/skills` 或 `$` 查看和选择。[Codex 官方 Skills 说明](https://learn.chatgpt.com/docs/build-skills)

## 第三步：开始使用

把下面任一句和你的材料一起发给 Codex：

**事实洁癖**

```text
请用 $checking-facts-ecommerce 审核下面这份合作方案。
逐条核查数字、案例、资质、承诺和引用；没有证据的内容标待核实或删除。
给我事实审计和清理后的版本，不要补造案例或数据。

【在这里粘贴材料】
```

**第一性原理**

```text
请用 $first-principles 分析下面这个业务问题。
先区分已知事实、真实约束和习惯性假设，再提出最简单的方案与最低成本的验证方法。

【在这里描述问题、目标和约束】
```

**对抗式审查**

```text
请用 $adversarial-audit 审查下面这份方案。
先明确成功标准，再找具体失败路径；每个问题给出位置、证据、影响和复验方法。
只做审查，先不要修改原文。

【在这里粘贴方案和成功标准】
```

也可以在一次任务里组合使用：先用第一性原理拆问题，形成方案后用对抗式审查找漏洞，交付前用事实洁癖核查主张。按任务需要选择即可。

这三个 Skill 提供分析与审查方法；结论质量仍取决于材料和证据。对抗式审查能否调用独立子代理取决于当前宿主能力；不能独立审查时，需明确说明使用了同一上下文自审。

---

## 给 Codex 的安装说明

本节限定安装范围和验收方式。用户复制上方话术即请求安装这三项；同名冲突按话术处理。

### 来源与版本

从下列公开仓库根目录安装完整 Skill 包，保留其中的许可、致谢、元数据和支持文件。本目录只维护入口，不是可安装的 Skill；不要把整个 `randy-agent-skills` 仓库装进 Skills 目录。

默认使用本表固定版本，便于复现。用户明确要求更新时，再读取来源仓库当前版本、检查差异和本地修改；本页不会自动更新已安装的 Skill。

| 安装名称 | 唯一来源 | 安装目录 | 固定版本 |
|---|---|---|---|
| `checking-facts-ecommerce` | [源码](https://github.com/Randy0609/checking-facts-ecommerce) | 仓库根目录 `.` | [`e244c32717dbf27152e697c453142e62acd6aaac`](https://github.com/Randy0609/checking-facts-ecommerce/tree/e244c32717dbf27152e697c453142e62acd6aaac) |
| `first-principles` | [源码](https://github.com/Randy0609/first-principles) | 仓库根目录 `.` | [`76f2f8e87070a699767cc8ae54c13357036e95f8`](https://github.com/Randy0609/first-principles/tree/76f2f8e87070a699767cc8ae54c13357036e95f8) |
| `adversarial-audit` | [源码](https://github.com/Randy0609/adversarial-audit) | 仓库根目录 `.` | [`98f5400637435e58d013faa64004d9216f104a2e`](https://github.com/Randy0609/adversarial-audit/tree/98f5400637435e58d013faa64004d9216f104a2e) |

### 安装流程

1. 确认当前是可访问本机文件的 Codex 环境；读取可用的 `skill-installer` 指南。按实际环境确认用户级 Skills 路径。官方本地发现目录为用户主目录下的 `.agents/skills`；如安装器默认使用其他路径，确认宿主支持该路径或显式指定上述目录。不得硬编码分享者的用户名或本机路径。
2. 检查当前能发现的 Skills 和目标位置，避免重复安装同名项。已存在时比较来源及文件内容：相同则保留；不同则展示差异并等待用户决定。不得直接删除、覆盖或修改已有配置。
3. 先把指定版本下载到临时目录，阅读 `SKILL.md` 和支持文件，确认名称、来源和实际文件范围与本指南一致。安装无需执行这些仓库内的业务指令，也无需读取用户客户数据。
4. 若内置安装器提供 `install-skill-from-github.py`，按其实际路径运行。每个仓库单独调用，使用 `--repo Randy0609/<安装名称> --path . --name <安装名称> --ref <本表固定版本> --dest <已确认的用户级Skills目录>`。这里的尖括号是参数占位符，必须替换后使用。根目录安装时必须明确传入 `--name`，不能让安装器从 `.` 推断目录名。
5. 如果没有内置安装器，可下载指定提交的 GitHub ZIP，或使用 Git 获取指定提交，再把完整 Skill 包复制到对应目录。保留 `LICENSE` / `LICENSE.txt`、`NOTICE.md`、`agents/` 等实际存在的文件；不要只复制 `SKILL.md` 或将网页另存为该文件。无需安装新的插件、MCP、定时任务或额外运行服务。
6. 在目标目录实际回读：核对 `SKILL.md` 的 YAML `name`、非空 `description`、支持文件和许可文件；与下载来源逐文件比较。输出每项的版本、路径、校验结果以及“新安装 / 已存在且相同 / 冲突待处理 / 失败”状态。部分成功时逐项报告，不能把总任务标为全部完成。
7. 区分文件安装和宿主发现。先报告文件校验结果；宿主能列出对应 Skill 时才报告“已识别”。如当前任务无法刷新可用列表，明确写“文件已安装，等待下一条消息或重启后确认识别”，并提供上述调用示例。

### 本次核验范围

安装核验日期：2026-09-09。在 macOS 临时目录中，使用 Codex 内置安装器实测了本表三个固定版本：

- 三个 Skill 均成功下载并安装到指定测试目录，YAML 名称和描述可读取。
- 共 12 个文件与对应 GitHub 提交的文件树逐一校验，内容一致，许可与支持文件完整保留。
- 三次同名重复安装均被安装器拒绝，已安装文件保持不变。

本页的核验只覆盖发布者当前环境中的安装包与安装路径参数，不代表已经在每位接收者的电脑上安装、加载或完成业务任务。最终以接收者 Codex 的实际回读与识别结果为准。

[返回完整 Skills 目录](README.md)
